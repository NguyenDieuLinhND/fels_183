<h1><%= t "user_method.edit" %> <%= resource_name.to_s.humanize %></h1>
<div class="col-md-6 col-md-offset-3">
  <%= form_for resource, as: resource_name,
    url: registration_path(resource_name), html: {method: :put} do |f| %>
    <b><%= devise_error_messages! %></b>

    <div class="field">
      <%= f.label :email %><br>
      <%= f.email_field :email, autofocus: true, class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :password %> <i><%= t "warning.change_password" %></i><br>
      <%= f.password_field :password,
        autocomplete: "off", class: "form-control" %>
      <% if @minimum_password_length %>
        <br>
        <em>
          <%= @minimum_password_length %> <%= t "warning.character_minimum" %>
        </em>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %><br>
      <%= f.password_field :password_confirmation, autocomplete: "off",
        class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :current_password %> <i><%= t "warning.need_password" %></i><br>
      <%= f.password_field :current_password, autocomplete: "off",
        class: "form-control" %>
    </div>
    <p><%= t "user_option.change_picture" %></p>
    <span class="picture">
      <%= f.file_field :avatar %>
    </span>

    <div class="actions">
      <%= f.submit t("user_option.update"),
        class: "btn btn-md btn-primary form-control" %>
    </div>
  <% end %>

  <h3><%= t "user_option.cancel_account" %></h3>

  <p>
    <%= button_to t("user_option.cancel_account"),
      registration_path(resource_name), data: {confirm: t("warning.sure")},
      method: :delete, class: "btn btn-danger" %>
  </p>

  <strong><%= link_to t("user_option.back"), :back %></strong>
</div>
