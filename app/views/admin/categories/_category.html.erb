<tr>
  <th><%= category.name %></th>
  <th><%= category.words.size %></th>
  <th>
    <%= link_to t("btn.edit"), edit_admin_category_path(category.id),
      class: "btn btn-primary" %>
  </th>
</tr>
